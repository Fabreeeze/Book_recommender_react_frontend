"use client";

import React from 'react';

const Header = () => {
    return (
        <div className="container flex flex-col justify-between items-center mx-auto max-w-xl  p-8">
            <h1 className="text-3xl font-semibold text-gray-800 mb-6">Book Library Search</h1>
        </div>

    );
};

export default Header;
